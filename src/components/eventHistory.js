import { View, Text } from 'react-native'
import React from 'react'
import { fetchEvents } from '../api/firestore'

export default function EventHistory() {
  return (
    <View>
      <Text>eventHistory</Text>
    </View>
  )
}